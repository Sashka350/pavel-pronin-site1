# Инструкции для работающего агента (opencode)

Этот файл читается при каждом запуске сессии в этой папке. Соблюдай его всегда.

## Ключевые правила

1. **Обязательно комиться.** После каждого осмысленного изменения делай git-коммит
   и пуши в `main`. Коммит — неотъемлемая часть завершения задачи, а не приятный бонус.
   Стиль сообщений: короткое описательное предложение (без точки в конце), по-русски,
   в том же стиле, что и в истории репозитория.

2. **Всегда проверяй мобильную версию.** Сайт mobile-first. Любое новое изменение
   должно выглядеть корректно и на мобильном, и на десктопе. После вёрстки/правки
   проверяй адаптив (узкие экраны ≤ ~390px и десктоп). Не ломай мобильное меню,
   сетку карточек и типографику на маленьких экранах.

## Контекст проекта

- Сайт-витрина театрального режиссёра и педагога Павла Пронина.
- Стек: Vite (multi-page), чистые HTML/CSS/JS, без фреймворков UI.
- Сборка: `npm run build` (Vite) + генераторы страниц спектаклей/педпроектов + sitemap/robots.
- Статические подстраницы генерируются скриптами в `scripts/` на stage postbuild.

## Публикация

- Публичный сайт: https://sashka350.github.io/pavel-pronin-site/
- Репозиторий: https://github.com/Sashka350/pavel-pronin-site (public)
- Автодеплой: push в `main` → GitHub Actions собирает и публикует на Pages.
- Базовый путь = `/pavel-pronin-site/` (задан в `vite.config.js` через `BASE_PATH`).
  В коде используй `import.meta.env.BASE_URL` (JS) и `withBase()` (генераторы).
  НЕ вписывай вручную жёсткие пути `/pavel-pronin-site/` в новые места —
  используй переменные базы. В статических HTML внутренние ссылки уже
  префиксованы base-путем.
- Адрес сайта для canonical/og/sitemap задаётся через `SITE_URL` в
  `scripts/page-template.js` (по умолчанию https://sashka350.github.io/pavel-pronin-site).

## Важные пути к файлам

- `vite.config.js` — base, входы (multi-page).
- `scripts/page-template.js` — общий шаблон (head/header/footer), BASE, SITE, withBase().
- `scripts/generate-performances.mjs`, `scripts/generate-pedagogy.mjs` — генераторы страниц.
- `src/data/` — данные контента (performances, pedagogy, inscenizations).
- `src/css/` — стили (variables, base, layout, components, pages).
- `src/js/` — сценарии (catalog, pedagogy, gallery, theme, nav и т.д.).
- `src/main.js` — входной JS.
- `ЧЕК-ЛИСТ.md` — статус проекта; обновляй при завершении работ.

## Прочее

- Не добавляй комментарии в код без необходимости.
- Соблюдай существующий стиль кода и структуру импортов.
- После правок данных в `src/data/` — пересобирай (`npm run build`), чтобы
  обновились сгенерированные страницы.